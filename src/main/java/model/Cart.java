package model;
import java.util.ArrayList;import utility.Logger;

public class Cart {	ArrayList<Product> cart;		public Cart() {		cart = new ArrayList<Product>();	}		public ArrayList<Product> getCartItems() {		return cart;	}		public void addItem(Product item) {		if(cart != null) {			cart.add(item);					}	}		public ArrayList<Product> updateStocks(ArrayList<Product> cart, ArrayList<Product> products) {		Product item = null;		String id = null;		int quantity = 0;		int stocks;				Logger.log("Updating Product Stocks...");				for(int index=0; index < cart.size(); ++index) { //for every item in the cart get its quantity			item = cart.get(index);			id = item.getId();			quantity = item.getQuantity();						System.out.println("SEARCHING PRODUCT LIST FOR [PRODUCT_ID: " + id + "]");						// find the item in the product list			for(Product p : cart) {				if(id.equals(p.getId())) {					System.out.println("\nITEM FOUND!");					System.out.println("ID: " + p.getId());					System.out.println("Stocks: " + p.getItem_stocks());					stocks = p.getItem_stocks() - quantity;					p.setItem_stocks(stocks);					System.out.println(p.getId() + " Stock Quantity Updated: " + p.getItem_stocks());					break;				}			}		}				return products;	}		public void logProductsStock(ArrayList<Product> products) {		// display all the product stocks		System.out.println("\nProduct List::::::::::::::::::::::::::::::::::::::::::::");		for(Product p : products) {			System.out.println("ID: " + p.getId());			System.out.println("Stocks Left: " + p.getItem_stocks() + "\n");		}		System.out.println("::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::::");	}
}
